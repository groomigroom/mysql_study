25번xxxxx

select C.광고매체명, B.광고명, A.광고시작일자
from 광고게시 A, 광고 B, 광고매체 C,
( ㄱ ) D
where A.광고시작일자 = D.광고시작일자
and A.광고매체ID = D.광고매체ID
and A.광고ID = B.광고ID
and A.광고매체ID = C.광고매체ID
order by C.광고매체명;

select D.광고매체ID, min(D.광고시작일자) as 광고시작일자
from 광고게시 D
where D.광고매체ID = C.광고매체ID
group by D.광고매체ID;

select 광고매체ID, min(광고시작일자) as 광고시작일자
from 광고게시
group by 광고매체ID;

select min(광고매체ID) as 광고매체ID, min(광고시작일자) as 광고시작일자
from 광고게시
group by 광고ID;

select min(광고매체ID) as 광고매체ID, min(광고시작일자) as 광고시작일자
from 광고게시


------------------------------

52번 문제

select COL1, sum(COL2)
from T1	
group by COL1
union all
select null, sum(COL2)
from T1
order by 1 asc;

select COL1, sum(COL2)
from T1
group by grouping sets (COL1)
order by 1 asc;

select COL1, sum(COL2)
from T1
group by rollup (COL1)
order by 1 asc;

select COL1, sum(COL2)
from T1
group by cube (COL1)
order by 1 asc;


-------------------------------------------------------------------------

49번

create table 게시판 (
	게시판ID varchar(50) primary key,
	게시판명 varchar(50),
	등록일시 date,
	사용여부 boolean
);

create table 게시글 (
	게시글ID varchar(50) primary key,
	게시판ID varchar(50),
	제목 varchar(50),
	내용 varchar(50),
	등록일시 date,
	등록자명 varchar(50),
	삭제여부 boolean
);

select A.게시판ID, A.게시판명, count(B.게시글ID) as CNT
from 게시판 A, 게시글 B
where A.게시판ID = B.게시판ID(+)
and B.삭제여부(+) = 'N'
and A.사용여부 = 'Y'
group by A.게시판ID, A.게시판명
order by A.게시판ID;

1번

select A.게시판ID, A.게시판명, count(B.게시글ID) as CNT
from 게시판 A left outer join 게시글 B
on (A.게시판ID = B.게시판ID and B.삭제여부 = 'N')
where A.사용여부 = 'Y'
group by A.게시판ID, A.게시판명
order by A.게시판ID;

2번

select A.게시판ID, A.게시판명, count(B.게시글ID) as CNT
from 게시판 A left outer join 게시글 B
on (A.게시판ID = B.게시판ID and A.사용여부 = 'Y')
where B.삭제여부 = 'N'
group by A.게시판ID, A.게시판명
order by A.게시판ID;

3번

select A.게시판ID, A.게시판명, count(B.게시글ID) as CNT
from 게시판 A left outer join 게시글 B
on (A.게시판ID = B.게시판ID)
where A.사용여부 = 'Y'
and B.삭제여부 = 'N'
group by A.게시판ID, A.게시판명
order by A.게시판ID;

-------------------------------------------------------------------------


백엔드용

create user 'developer'@'*' identified by 'P!ssw0rd';

-------------------

grant select, update, delete, insert
on board.*
to 'developer'@'*';

-------------------


TEXT 타입은 최대 65,535바이트(약 64KB)의 가변 길이 긴 문자열 데이터를 저장하기 위한 대용량 문자열 데이터 타입입니다. 
글자 수(길이)를 따로 지정하지 않고 사용하며, 블로그 본문이나 상세 내용처럼 긴 글을 저장할 때 쓰입니다. 
주요 특징
용량 크기: 최대 64KB(65,535바이트) 저장 가능
길이 지정 불필요: VARCHAR(255)처럼 괄호 안에 크기를 적지 않음
종류 세분화: 더 큰 용량이 필요할 때 TINYTEXT, MEDIUMTEXT, LONGTEXT 사용 가능
VARCHAR와의 차이점
저장 위치: TEXT는 임시 메모리(인메모리) 버퍼 공간을 쓰지 않고 별도 영역에 저장되어 속도가 상대적으로 느릴 수 있음
기본값 제한: TEXT 타입은 DEFAULT 기본값을 가질 수 없음



-------------------

board_number integer not null auto_increment

-------------------

favorite_count INT NOT NULL DEFAULT 0

-------------------



--------------------------------------------------------------------

단일행, 다중행, 단일 컬럼, 다중 컬럼 서브쿼리의 의미?    +9                    모두 보기서브쿼리의 결과 분류는 반환되는 행(Row)과 컬럼(Column, 열)의 개수에 따라 결정되며, 주요 유형은 단일행, 다중행, 다중 컬럼(다중열) 서브쿼리입니다. (참고로 '단일 컬럼'은 결과 열이 1개인 기본적인 형태를 뜻합니다.)서브쿼리 유형별 의미단일행 서브쿼리 (Single-Row Subquery)서브쿼리의 실행 결과가 오직 1개의 행(Row)만 반환하는 형태입니다.= >, < 등 일반적인 단일행 비교 연산자를 사용합니다.다중행 서브쿼리 (Multi-Row Subquery)서브쿼리의 실행 결과가 2개 이상 다수의 행(Row)을 반환하는 형태입니다.IN, ANY, ALL, EXISTS 같은 다중행 전용 연산자를 함께 사용해야 합니다.단일 컬럼 서브쿼리 (Single-Column Subquery)서브쿼리의 실행 결과로 반환되는 컬럼(열)의 개수가 1개인 형태입니다.데이터 값 하나 혹은 여러 행의 단일 속성 값을 조회할 때 쓰입니다.다중 컬럼 서브쿼리 (Multi-Column Subquery)서브쿼리의 실행 결과로 2개 이상의 컬럼(열)을 동시에 반환하는 형태입니다.메인 쿼리의 조건절에 비교하는 컬럼 개수와 서브쿼리가 반환하는 컬럼 개수가 일치해야 합니다.이해를 돕기 위해 각 서브쿼리 유형별 SQL 예제 코드나 사용하는 연산자 종류를 더 자세히 설명해 드릴까요?티스토리서브쿼리에 대해 알아보자 1편 (feat.단일행, 다중열, 다중행 서브쿼리)2023. 2. 5. — 1) 단일행 서브쿼리: 하나의 값만 리턴. · 2) 다중행 서브쿼리: 여러 개의 행을 리턴. · 3) 다중행 열 서브쿼리: 여러개의 열로 구성된 여러 개의 행인 ...YouTube·아이리포10m[SQLD 모든 것] 20. 중첩 서브쿼리 | 단일행 | 다중행 | 다중칼럼 | 아이리포YouTube·SQL전문가 정미나1m서브쿼리 개념, 이 영상 하나로 완벽 종결!티스토리SQL - 다중 행, 다중 열 서브 쿼리 - 냉유's Log - 티스토리2017. 12. 10. — 서브 쿼리에서 여러 행이 검색되는 쿼리문이다. 연산자를 제외하면 단일 행 서브 쿼리와 같다. 기본 구조. SELECT [DISTINCT] ...티스토리단일행 서브쿼리(Single Row SubQuery) / 다중행 서브쿼리(Multi ...2023. 11. 21. — // 단일행 서브쿼리는 다중행 비교연산자를 사용할순 있지만 다중행 서브쿼리는 불가능하다. 다중 컬럼 서브쿼리. 비교 컬럼 갯수가 같아야한다. 현재 ...티스토리[SQL] 단일행 ,다중행 서브쿼리/상호관련 서브쿼리 - 착해지는 중 입니다.2024. 5. 8. — 1. 서브쿼리는 반드시 괄호로 묶음 · 2. 조건식에서는 가독성을 위해 비교 조건의 오른쪽에 배치 · 3. 서브쿼리의 결과 건수에 따라 단일행 비교 연산자와 ..티스토리[DB] 서브쿼리 (단일행, 다중행, 다중열, WITH절) - 내가 한 노력들2021. 12. 1. — 서브쿼리의 실행 결과가 하나의 행으로 나오는 서브쿼리를 단일행 서브쿼리라고 합니다. ... 다중행 서브쿼리는 다중행 연산자를 사용해야 합니다. 다중행 ...티스토리[DB] SQL - 단일 행, 다중 행, 다중 열 서브쿼리 개념 정리 - planting code2025. 4. 1. — 다중 열 서브쿼리란? 서브쿼리의 결과가 2개 이상의 컬럼(열)을 반환한다. 주로 IN , = ANY , EXISTS 와 같은 연산자를 사용한다. (부서 ID, 직...데이터리안MySQL 서브쿼리 연산자 (비교 연산자, IN, ANY/SOME, ALL, EXISTS)2025. 6. 18. — 데이터 분석 소식을 매월 받아보세요 연산자 사용 조건 설명 비교 연산자 단일행 서브쿼리 = , > , < , >= , <= , <> 등 비교연산자를 이용해 단일행...티스토리[SQL] 서브쿼리 (SUBQUERY)2022. 10. 4. — 서브쿼리가 사용 가능한 곳 다중행서브쿼리 3) [단일행] 다중열 서브쿼리 : 서브쿼리를 수행한 결과값이 여러 열일 때 조회 결과값은 한 행이지만 나열된 컬럼수가 ...    AI 답변에 오류가 있을 수 있습니다. 자세히 알아보기


mmmmmmmmmmm


제시된 해설처럼 3차 정규화까지 마쳤을 때 총 7개의 엔티티가 도출되는 과정은 다음과 같습니다.
제 1, 2, 3 정규화 원칙에 따라 기존 엔티티들이 어떻게 분리되는지 단계별로 명쾌하게 설명해 드리겠습니다.

## 1. '학생' 엔티티의 정규화 (4개 엔티티 분리)
기존 '학생' 엔티티는 여러 종류의 정보가 섞여 있어 정규화 대상이 됩니다.

* '학과' 엔티티 분리 (제3정규화)
* 학생 테이블 내에 학과번호와 학과명이 있습니다.
   * 학번 $\rightarrow$ 학과번호 $\rightarrow$ 학과명 구조로, 기본키가 아닌 일반 속성 간에 종속 관계(이행적 함수 종속)가 존재합니다.
   * 따라서 [학과](학과번호, 학과명) 엔티티로 분리됩니다.
* '교수' 엔티티 분리 (제3정규화)
* LAB실이용승인교수번호에 따라 LAB실이용승인교수명이 결정됩니다.
   * 이 역시 이행적 함수 종속이므로 [교수](교수번호, 교수명) 엔티티로 분리됩니다.
* 'LAB실이용신청' 엔티티 분리 (제1정규화 - 반복 그룹 제거)
* LAB실이용신청일1, 2, 3, 4와 같이 동일한 속성이 여러 개 나열되어 있는 반복 그룹 상태입니다.
   * 반복되는 속성을 별도의 엔티티로 독립시켜야 하므로 [LAB실이용신청] 엔티티가 도출됩니다.
* 남은 [학생] 엔티티
* 위의 속성들이 빠져나가고 순수한 학생 정보(학번, 성명 등)만 남은 [학생] 엔티티가 유지됩니다.

💡 결과 (4개): 학생, 학과, 교수, LAB실이용신청


## 2. '도서대출' 엔티티의 정규화 (3개 엔티티 분리)
문제 조건에서 "하나의 대출자(학생)에 대해 하나의 대출번호로 여러 개의 도서를 대출/반납한다"라고 명시되어 있습니다. 즉, 영수증 하나(대출번호)에 여러 책이 찍히는 구조입니다.

* '대출도서' 엔티티 분리 (제1정규화 - 반복 그룹 제거)
* 하나의 대출번호에 여러 도서가 매핑되므로, 대출 상세 내역에 해당하는 [대출도서] (또는 대출상세) 엔티티가 분리되어 나와야 합니다. (이 엔티티에는 대출번호, 대출도서번호, 반납일자 등이 포함됩니다.)
* '도서' 엔티티 분리 (제2정규화 - 부분 함수 종속 제거)
* 대출도서번호에 종속되는 도서 고유 정보인 도서명, 출판사명, 출판년도, 대표저자명, ISBN 등은 대출 행위와 상관없는 도서 자체의 속성입니다.
   * 따라서 핵심 마스터 테이블인 [도서] 엔티티로 완전히 분리됩니다.
* 남은 [대출] 엔티티
* 도서 상세 정보가 빠져나가고, 대출 행위의 큰 틀만 담은 [대출] 엔티티(대출번호, 대출자번호, 대출일자 등)가 남게 됩니다.

💡 결과 (3개): (도서)대출, 대출도서, 도서


## 짚고 넘어가기 (최종 요약)
각 영역에서 도출된 엔티티들을 모두 더하면 다음과 같습니다.

   1. 학생 (기본 정보)
   2. 학과 (학생에서 분리)
   3. 교수 (LAB실 신청 승인 교수 정보 분리)
   4. LAB실이용신청 (반복 속성 분리)
   5. (도서)대출 (대출 마스터 정보)
   6. 대출도서 (1개 대출번호에 여러 도서가 속하므로 분리된 상세 정보)
   7. 도서 (도서 고유 마스터 정보)

따라서 총 7개의 엔티티가 도출되므로 답은 3번이 됩니다.

추가로 특정 정규화 단계(1, 2, 3차)에서 어떤 속성이 어떻게 쪼개지는지 더 상세한 예시가 필요하시면 언제든 말씀해 주세요!
