sql에서 grouping sets((DNAME, JOB), DNAME)의 의미
GROUP BY GROUPING SETS ((DNAME, JOB), DNAME)은 
전체 데이터를 지정한 개별 집합별로 각각 그룹화하여 DNAME과 JOB을 
모두 묶은 결과와 DNAME만 묶은 결과를 하나로 합쳐서(UNION) 보여줍니다.
동작 방식 및 의미
(DNAME, JOB) 그룹: 부서(DNAME)와 직무(JOB)를 동시에 기준으로 삼아 데이터를 집계합니다.DNAME 그룹: 부서(DNAME)별로만 데이터를 모아서 집계합니다 (부서별 소계 역할).이 두 가지 집계 결과를 UNION ALL로 합친 것과 정확히 같은 결과가 나옵니다.비교 예시결과적으로 아래 두 쿼리는 같은 역할을 합니다.sql-- GROUPING SETS 사용
SELECT DNAME, JOB, COUNT(*) 
FROM EMP 
GROUP BY GROUPING SETS ((DNAME, JOB), DNAME);
코드를 사용할 때는 주의가 필요합니다.sql-- UNION ALL 사용 (동일한 의미)
SELECT DNAME, JOB, COUNT(*) FROM EMP GROUP BY DNAME, JOB
UNION ALL
SELECT DNAME, NULL AS JOB, COUNT(*) FROM EMP GROUP BY DNAME;
코드를 사용할 때는 주의가 필요합니다.만약 원하신다면 예시 데이터와 실제 출력 결과를 추가로 보여드릴까요?티스토리Grouping sets - 그룹함수 - pakker - 티스토리2021. 3. 29. — grouping sets 함수 사용시 union all 을 사용한 일반 그룹함수를 사용한 sql과 같은 결과를 얻을 수 있으며, 괄호로 묶은 집합 별로(괄호 내는...블로그[MSSQL] 그룹 함수(ROLLUP, CUBE, GROUPING SETS) - 네이버 블로그2019. 12. 26. — 위의 예제에서는 DNAME 별 JOB 에 대한 소계와 JOB 별 DNAME 에 대한 소계 그리고 총계가 생겨난 것을 볼 수 있다. ​. - GROUPING SET...티스토리[DATABASE] 그룹함수란? ROLLUP, CUBE, GROUPING SETS 란? - 꽁담2018. 11. 14. — AGGREGATE FUNCTION. GROUP AGGREGATE FUNCTION 이라고도 부릅니다. COUNT, SUM, AVG, MAX, MIN 외 각종 집계 ...    AI 답변에 오류가 있을 수 있습니다. 자세히 알아보기
